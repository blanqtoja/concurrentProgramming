<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vms="clr-namespace:ViewModel;assembly=ViewModel"
        
        mc:Ignorable="d"
        Title="Snooker" Height="450" Width="800">

    <Grid Background="#00aa00">
        <Grid.DataContext>
            <vms:MainViewModel/>
        </Grid.DataContext>

        <TextBox HorizontalAlignment="Center" VerticalAlignment="Bottom" TextWrapping="Wrap" Width="120" Text="{Binding Path=BallsAmount, Mode=TwoWay}"/>
        <Button Content="Start" HorizontalAlignment="Center" VerticalAlignment="Bottom" Command="{Binding StartCommand}"/>

        
        <ItemsControl  ItemsSource="{Binding Balls}" Grid.ColumnSpan="2" Margin="10,10,10,10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Width="700" Height="300" Background="#232323"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Height="{Binding Radius}" Width="{Binding Radius}" Stroke="BlueViolet" Fill="#aaffff"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
        
</Window>
